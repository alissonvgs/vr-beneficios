spring:
  application:
    name: autorizador
  datasource:
    url: jdbc:mysql://localhost:3306/miniautorizador
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
    operations-sorter: method
    tags-sorter: alpha
